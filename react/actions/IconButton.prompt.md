Square icon-only button for toolbars and table row actions.

```jsx
<IconButton label="Aktualisieren"><i data-lucide="refresh-cw"></i></IconButton>
```

Always square (24/32/40px). Use `ghost` inside dense table rows so the grid rules stay dominant.
