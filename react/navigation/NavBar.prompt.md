Top-level application navigation. The wordmark block is set in type — the system ships no logo file.

```jsx
<NavBar product="BETRIEBSLEITSTAND" items={['Übersicht','Umläufe']} value={v} onChange={setV} right={<StatusLed status="ok" label="ONLINE" />} />
```
